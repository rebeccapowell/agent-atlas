# Alex — History

## Core Context
Agent Atlas React UI served from `src/Atlas.Host/wwwroot/`. Stack: React + shadcn/ui. The UI is a read-only capability map of registered MCP tools and APIs. Calls AllowAnonymous catalog endpoints (`/v1/apis`, `/v1/tools`). No auth required for browsing. Requested by Rebecca Powell.

## Learnings
_Append new entries below as work progresses._
