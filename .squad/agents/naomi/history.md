# Naomi — History

## Core Context
Agent Atlas is an open source enterprise MCP gateway. My domain: ASP.NET Core 10, .NET Aspire 13.x, MCP server (ModelContextProtocol.AspNetCore 1.0.0), execution engine, catalog loader, JWT/OIDC via Keycloak. Build: `dotnet build src/Atlas.AppHost/Atlas.AppHost.csproj`. Run: `aspire run --project src/Atlas.AppHost`. Requested by Rebecca Powell.

## Learnings
_Append new entries below as work progresses._
